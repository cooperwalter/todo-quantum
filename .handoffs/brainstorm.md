---
stage: brainstorm
timestamp: 2026-06-10T22:26:35Z
sha: 762af0b
decided: ["Edit field reconstructs the task as text: title + existing metadata as live chips (full round-trip); save re-parses the field text as the single source of truth","Date serialization: relative when near (yesterday/today/tomorrow), else absolute (jun 12), explicit year (jun 3 2026) when outside the next 12 months; parser gains explicit-year support","Last-wins chip overwrite in BOTH add and edit, replacing first-token-wins; overwritten chip's text is removed from the field","Sealing rule: displaced text is removed only when the newer token is followed by a space or the field is submitted; until then it renders demoted and is restored if the transient parse disappears (prevents prefix-parse data loss, e.g. 'satchel' through 'sat')","Reverted ranges move INTO parse(input, now, reverted?) so demoted literal title words can never be chipped nor displaced; round-trip stays lossless for titles like 'Email #invoices tomorrow'","Displacement operates on pre-merge tokens: a second bare time displaces only the old time range inside a merged date+time chip","Deleting a chip's text in edit clears that field on save (field text is the truth)","Approach A: extract shared CONTROLLED ParsedInput component (value/onChange, inputRef, onKeyDown caller-first interception, initialReverts, frozen now) from CommandBar; new pure serialize.ts","Esc two-tier with deterministic session-chip definition: a chip whose (kind, parsed value) is absent from the baseline parse of initialValue; Esc reverts most recent session chip, else cancels","Blur SAVES (same as Enter; keeps today's onBlur=saveEdit behavior, user-confirmed); blur with empty title cancels","Open-edit gesture: Enter or single title click (matches current code); no double-click","Remove the three-dots overflow menu from TaskRow; snooze keyboard-only (1/2/3); narrowed principle stated: keyboard primary, mouse covers complete/open-edit/select","One edit action carries all changed fields; single undo restores the whole edit","Contract supersession flagged: /ql-plan must update quantum.json first-wins contract to last-wins-with-displacement and spec must rewrite affected FR-9 criteria","byMonthDay excluded from round-trip property domain (no grammar can express it)"]
rejected: ["Approach B (edit in the command bar): context jump, breaks approved inline-edit model, blocks capture while editing","Approach C (parse-on-save only, no chips in edit): no live feedback, metadata invisible, contradicts round-trip decision","Edit-only overwrite (keep first-wins in add): rejected for symmetry","Demote overwritten chip text to literal title words: titles accumulate junk; removal keeps WYSIWYG","Deletion never clears fields: breaks field-text-is-the-truth","Cancel-on-blur: rejected by user; save-on-blur kept","Eager per-keystroke displacement removal: rejected after design review (transient prefix parses would silently delete due dates)"]
risks: ["Refactor extracts machinery from CommandBar, the app's most polished component — existing e2e + visual gate cover it; ParsedInput must remain controlled to keep useKeymap typeahead/barRef/command-mode ARIA working","Chip mirror must render correctly at row width","Parser last-wins supersedes a recorded quantum.json contract and FR-9 tests — halt-and-propose satisfied by this design; plan stage must update them explicitly","Round-trip fidelity for past dates depends on new explicit-year parsing; missing it silently rewrites due dates on edit","Removing the overflow menu drops the only pointer path to snooze (accepted, principle narrowed explicitly)"]
files: ["docs/plans/2026-06-10-edit-token-parsing-design.md"]
remaining: ["Exact whitespace-collapse rules during displaced-text removal (implementation latitude)","Spec should enumerate the serializer's canonical token order: title, date, time, recurrence, list, priority"]
---

Feature extends the in-progress command-bar app (quantum.json US-001..US-018). quantum.json userIntent is immutable (original app intent); this feature's verbatim intent + two mid-brainstorm clarifications (chip overwrite; remove three-dots menu / keyboard-first) are in the design doc front-matter. Advisory design review: 11 findings (2 critical / 3 major / 6 minor), all resolved in the doc; snapshot in .handoffs/design-review-findings.json.
