---
stage: brainstorm
timestamp: 2026-06-09T18:59:58Z
sha: 7e7acaf
decided: ["Approach A: Command Bar Is the App — one omnipresent input that is both NL quick-add and ('>' prefix) command palette; views are flat filters beneath it","Target user: busy professionals; core problem: capture friction; success = speed benchmarks (capture <2s, daily triage <1min, today's plan at load with zero clicks)","Local-first, no backend: single versioned JSON document in localStorage, schemaVersion for migrations, debounced verified writes","v1 features: NL quick-add with live parse chips + Esc chip-revert; Today/Upcoming/All/Done views ('g then t/u/a/d'); recurring tasks + one-key snooze (1/2/3 = tomorrow/next week/weekend); full keyboard nav + universal in-memory undo stack (max 50 inverse actions)","Overdue policy: gentle rollover — derived at read time (open + dueDate < today), subtly marked, never stored, no red guilt wall","Recurring completion spawns a new Task at next occurrence computed from today (no back-dated avalanche); monthly recurrence clamps to month end","Parser: keyword-anchored date tokens only (no bare-month false positives); deterministic with injected 'now'; chips carry source ranges","Architecture: React 19 + Vite, zero new runtime deps; framework-free core in src/lib (parser, recurrence, selectors, store with inverse-action undo, persistence); thin React shell on one useReducer + context; centralized useKeymap with context priority","Storage failure modes are loud: quota/private-mode → in-memory + banner; corrupt JSON → recovery-key stash + fresh start + notice; multi-tab last-writer-wins via storage events","Testing: unit-heavy Vitest on pure core with injected clocks (≥80% coverage); thin Playwright e2e on speed-benchmark journeys; visual gate (diff+axe+lighthouse at 375/768/1280) per UI story","Design system resolved via /lens-design (2026-06-09): direction \"Morning Edition\" — warm broadsheet editorial; Fraunces/Newsreader/JetBrains Mono; vermilion accent #A63A12 light / #E8703A dark; all token pairs WCAG AA verified; tokens in src/styles/tokens.css, contract names match quantum.json"]
rejected: ["Approach B (Things-style three-pane): familiar but capture is a modal away — fails the core capture-friction goal","Approach C (day-first triage desk): inverts priorities (planning over capture) and contradicts the gentle-rollover choice","A+B hybrid with persistent sidebar: rejected in favor of pure command-bar with compact view tabs","Out of scope v1 (explicit): collaboration, email/Slack/calendar integrations, subtasks/projects hierarchy, push/desktop notifications","Sync/accounts/backends, IndexedDB, state libraries (Redux/Zustand), persisted undo stack"]
risks: ["NL parser quality is load-bearing — a bad parse erodes capture trust; mitigated by live chips, Esc revert, keyword-anchored dates, exhaustive unit suite","Novel command-bar UX has weaker discoverability — mitigated by '?' cheatsheet, mouse parity, plain-text-always-captures rule","DESIGN-SYSTEM.md is still placeholder — visual gate and token contracts are unenforceable until /lens-design runs","Multi-tab last-writer-wins can lose a write in rare concurrent edits — accepted for single-user v1"]
files: ["docs/plans/2026-06-09-command-bar-todo-design.md"]
remaining: ["Spec decision: 'weekend' snooze target when today is already the weekend","Spec decision: does '>search' free-text filtering make v1 or fold into All view"]
---

 /lens-design completed after brainstorm; DESIGN-SYSTEM.md fully filled.
