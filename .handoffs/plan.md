---
stage: plan
timestamp: 2026-06-09T20:45:18Z
sha: 7e7acaf
decided: ["quantum.json: 18 stories, 73 tasks, branch ql/command-bar-todo; 10 waves (max parallel x5), plan-lint clean with zero same-wave file conflicts","DAG: US-001 foundation -> {002,003,005} parallel -> 004 -> 006 (shell+AppContext) -> 007 (bar+dev-deps) -> {008,009,011} -> {010,014,015} -> {012,013,016} -> 017 -> 018","App.tsx single-owner chain: 006 creates, then 007 -> 011 -> 010 -> 016 -> 017, each dep-ordered — never two same-wave owners","US-006 pre-stubs src/views/{Today,Upcoming,All,Done}View.tsx placeholders so view stories own only their file (engineered wave disjointness)","Route visual baseline (verification/visual.spec.ts-snapshots) owned only by render-changing stories 006/007/009/013, all dep-ordered; other UI stories capture interaction states in story-owned e2e/states-*.spec.ts files","AppContext (src/state/AppContext.tsx) created in US-006 over the US-004 reducer; barText shared there for US-015 filter; toast slice added by US-011","Contracts: design_tokens + routes carried from seed; added storage_keys (todo-quantum.v1, recovery prefix), commands.palette_set, keybindings (1/2/3/x/?), structural shared_types Task/Recurrence/AppData (owner US-001, definitionFile src/lib/types.ts) and Chip/ParseResult (owner US-002, src/lib/parser.ts) with shape+definition","Dev-dep trio (@testing-library/react, user-event, jsdom) lands in US-007 T-001; US-006 therefore uses e2e+visual gate only (no component tests) with justification notes","coverageThreshold 80 (from lens.config), staleThresholdMinutes 20; seed quantum.json archived to archive/2026-06-09-ql-initial-feature/","Snooze date targets live in src/lib/dates.ts (US-001) consumed by keymap (US-010) and TaskRow menu (US-012)","plan-review (advisory) found 12 findings (1 critical / 3 high / 5 medium / 3 low); ALL 12 applied: load() wired at AppContext init (US-006) fixing the critical seed-before-persistence bug; US-014/US-015 gained US-010 (and US-015 US-011) dependency edges; US-006 T-002 split into three tasks; dates.ts and AppContext got planner-added covering ACs; lighthouserc puppeteerScript seeds 100 tasks for FR-48"]
rejected: ["Single shared verification/visual.spec.ts edited by every UI story — caused same-wave conflicts on visual.spec.ts; replaced with story-owned e2e/states-*.spec.ts + baseline-owner chain","US-006 independent of libs (wave-1 parallel with US-001) — rejected because AppContext needs the US-004 reducer; shell now waits for store","Component tests in US-006 — rejected: @testing-library arrives in US-007; shell behavior covered by e2e/shell.spec.ts + visual gate","Toast mounted via separate portal story before CommandBar — rejected; US-011 depends on US-007 so the App.tsx owner chain stays linear","Putting snooze helpers in store.ts — rejected; pure date helpers belong in dates.ts (framework-free, shared by keymap and menu)"]
risks: ["NL parser quality is load-bearing; grammar table FR-1..10 + exhaustive unit suite is the mitigation (carried)","Focus-model regressions easy to introduce; useKeymap centralization + 3-context Cmd-Z tests (carried)","Coverage gate may pinch if React shell grows untested; component tests mandated US-007 onward (carried)","Multi-tab last-writer-wins accepted for single-user v1 (carried)","NEW: route-baseline churn — stories 007/009/013 re-bless the / baseline; if a re-bless lands without inspection the Pixel Law is violated. Baseline owners are dep-ordered to prevent races","NEW: jsdom component tests cannot assert pixels; all pixel evidence flows through story-owned playwright state specs + the gate","NEW: e2e specs share the dev server via run-visual-gate.sh; parallel worktree agents each start their own server on 5173 — possible port contention. quantum-loop worktree isolation should use per-agent ports; surface to ql-execute if it bites"]
files: ["quantum.json"]
remaining: []
---

Waves after fixes: 1:{001} 2:{002,003,005} 3:{004} 4:{006} 5:{007} 6:{008,009,011} 7:{010} 8:{012,013,014,015,016} 9:{017} 10:{018}. All 18 PRD stories with ACs verbatim plus two planner-added ACs (dates.ts, AppContext). Sprint contracts re-written post-fix. dag-validator annotations (prdSha, complexity, fileConflicts, dagValidation) from the pre-fix run remain valid for structure; fileConflicts list unchanged in membership.
