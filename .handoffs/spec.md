---
stage: spec
timestamp: 2026-06-10T22:40:46Z
sha: 762af0b
decided: ["Done tasks remain non-editable (open tasks only); FR-120 now has an explicit regression AC in US-106","Round-trip mismatch at edit-open falls back to title-only editing (parseEnabled=false, save updates title only) with a console error","Displaced-removal whitespace: seam collapses to a single space; live field never trimmed; trim only in extracted title","Serializer canonical token order: title, date, time, recurrence, list, priority","Displaced ranges are EXCLUDED from ParseResult.title exactly like chip ranges (FR-102) — makes US-102 safe to ship before US-105 and pins title semantics for round-trip (FR-109) and diff-save (FR-118)","Chip date display shows the year whenever the resolved date's year differs from now's year; formatDateDisplay gains an optional now parameter","Story numbering US-101..US-108 to avoid collision with existing quantum.json US-001..US-018","US-104 is a zero-behavior-change extraction gate (existing CommandBar unit + e2e pass unmodified) before US-105 adds sealing/displacement","Explicit-year grammar: 4-digit years 1970-2100, both word orders","Blur saves like Enter; blur with empty extracted title cancels; Enter with empty title blocks with inline feedback","Verify-in-browser ACs concretized as 'bash verification/run-visual-gate.sh / passes' per UI story","Overflow/menu CSS lives in src/components/TaskList.css (verified), not a TaskRow stylesheet — US-107 filePaths must include it","FR-102 explicitly supersedes prior PRD FR-9; plan stage must update quantum.json parse_result contract (add displaced) and parsing-rule contract"]
rejected: ["Blocking the editor on round-trip mismatch (error toast): locks user out; fallback chosen","Trusting a mismatched baseline parse silently: serializer bug could rewrite task data on save","Remove-token-plus-one-space whitespace rule: can leave double spaces; collapse-to-one chosen","Keeping displaced text in ParseResult.title until US-105 lands: would ship the explicitly-rejected junk-title behavior in the intermediate state","Restarting story numbering at US-001: would collide with in-repo quantum.json ids"]
risks: ["CommandBar extraction regression risk — gated by US-104 no-behavior-change criteria","Chip mirror at row width is new rendering territory — visual baselines looked at before blessing (US-108)","Last-wins supersedes recorded quantum.json contract + FR-9 tests — explicit plan-stage update required","Missing explicit-year parsing silently rewrites past due dates — US-101 ordered before US-103 round-trip property","Menu removal drops only pointer path to snooze — accepted, principle narrowed (Non-Goal 3)","US-106 is the densest story — plan stage should carve the store.test.ts multi-field-inverse AC into its own task (PRD §7 note)"]
files: ["tasks/prd-edit-token-parsing.md"]
remaining: []
---

PRD re-grounds against the feature's verbatim intent + both mid-brainstorm clarifications (quoted in PRD header). Six quantum.json.userClarifications entries logged. All cited paths grep-verified; [NEW] annotations on serialize.ts, ParsedInput.tsx/.css/.test.tsx, serialize.test.ts, e2e/edit-tokens.spec.ts. Advisory prd-review: 7 findings (0 critical / 2 major / 5 minor), ALL resolved in the PRD (title-exclusion of displaced ranges resolves both majors); snapshot in .handoffs/prd-review-findings.json. Lifecycle checklist fully resolved.
