---
stage: spec
timestamp: 2026-06-09T20:24:03Z
sha: 7e7acaf
decided: ["PRD: tasks/prd-command-bar-todo.md — 18 stories (US-001..005 pure lib, US-006..017 UI, US-018 e2e suite), FR-1..FR-48 contiguous, 9 non-goals","Weekend snooze (key 3) = next Saturday strictly after today, even on weekends (FR-33)","Search = All-view live filter from bar text only; Enter still captures (FR-20); global search is NG-7","Multi-tab external write clears undo+redo stacks with toast (FR-43)","Data export/import deferred — NG-6","Focus model (resolves review critical): two contexts BAR/LIST; down-arrow or Esc-on-empty-bar moves to LIST; unbound printable in LIST refocuses BAR and types (FR-15..17 with full Esc precedence chain)","Cmd-Z routing by FOCUS CONTEXT (fixes prd-review dead-keystroke gap): LIST focused = app undo always; BAR empty = app undo; BAR non-empty = native input undo (FR-37); redo stack cleared on new mutation (FR-38)","Recurrence: byMonthDay anchor field; next = smallest pattern date strictly after max(today, dueDate); monthly clamps; one open task per chain (FR-26..28)","Parser grammar enumerated as definitive token table FR-1..10 (keyword-anchored dates, first-wins per kind, !p1..3 only, single #list)","A11y designed in: aria-live toast, aria-describedby chip announcements, roving tabindex list, focus-trapped cheatsheet dialog (FR-44..47)","Speed proxies machine-verifiable: Enter-to-row <200ms, zero-click Today, keyboard-only 5-task triage, Lighthouse perf >=0.9 @100 tasks (FR-48, US-018)","order field = insertion counter only; manual reorder is NG-8 (FR-30)","Today keeps approved 3-section composition explicit in FR-21; Done ordering completedAt desc (FR-23); upcomingGroups selector omits empty groups (FR-22)","Toast story (US-011) precedes Snooze (US-012); Cheatsheet split into US-017 (after US-008/US-010); >help opens cheatsheet (FR-19)","Browser-verified AC defined as artifact: visual-gate screenshots at 3 breakpoints inspected and cited in completion evidence","New dev deps for component tests, all introduced in US-007: @testing-library/react, @testing-library/user-event, jsdom","Midnight-timer DST test mandated in US-013 (2026-03-08 and 2026-11-01, fake timers)"]
rejected: ["Weekend-snooze degrading to tomorrow or disabling key 3 — breaks key consistency",">find / dedicated search command — extra surface for marginal v1 value","Keeping undo stack best-effort across multi-tab reloads — risks clobbering the other tab's writes","Export-only escape hatch in v1 — deferred whole, keeps scope tight","FR alias/reserved-gap numbering hack — replaced with clean contiguous renumber after prd-review flagged reference integrity"]
risks: ["NL parser quality is load-bearing; grammar table FR-1..10 + exhaustive unit suite is the mitigation","Novel command-bar UX discoverability; mitigated by ? cheatsheet, >help, mouse parity, FR-11 capture-never-hijacked","Focus-model regressions are easy to introduce; useKeymap centralization + 3-context Cmd-Z tests are the mitigation","Coverage gate may pinch if React shell grows untested; component tests mandated in US-007..017","Multi-tab last-writer-wins can drop rare concurrent edits — accepted for single-user v1"]
files: ["tasks/prd-command-bar-todo.md"]
remaining: ["/ql-plan: dev-dep introduction (@testing-library trio) must land in US-007 before any other component-test story executes","/ql-plan: US-013 owns the canonical / visual baseline; earlier UI stories create interim baselines — plan baseline handoff between waves","/ql-plan: honor explicit dependencies US-011 before US-012; US-017 after US-008 and US-010; US-018 last"]
---

PRD re-grounds against verbatim userIntent (best features = NL quick-add/Today-Upcoming/recurring+snooze/keyboard+undo; pain points fixed = capture friction, guilt wall, trust/undo, silent data loss). Four spec answers in quantum.json.userClarifications. prd-review (advisory) found 14 findings (1 high / 6 medium / 7 low); ALL 14 applied to the PRD in-place before handoff — see .handoffs/prd-review-findings snapshot where each finding records its resolution.
